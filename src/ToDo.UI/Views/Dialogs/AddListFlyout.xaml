<Page x:Class="ToDo.Views.Dialogs.AddListFlyout"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:ToDo.Views.Dialogs"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:uen="using:Uno.Extensions.Navigation.UI"
	  xmlns:uer="using:Uno.Extensions.Reactive.UI"
	  xmlns:utu="using:Uno.Toolkit.UI"
	  xmlns:um="using:Uno.Material"
	  mc:Ignorable="d"
	  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<utu:NavigationBar Content="New List"
						   Style="{StaticResource MaterialNavigationBarStyle}">
		</utu:NavigationBar>

		<StackPanel Grid.Row="1">
			<TextBlock Text="List Name" />
			<TextBox Text="{Binding ListName, Mode=TwoWay}" />
		</StackPanel>

		<StackPanel HorizontalAlignment="Right"
					Orientation="Horizontal" Grid.Row="2">
			<Button Content="Cancel" uen:Navigation.Request="-"/>
			<Button Content="Create"
					Command="{Binding AddCommand}"/>
		</StackPanel>
	</Grid>
</Page>
